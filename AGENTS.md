# 프로토타입 개발 규칙

- 첫 번째 플레이 가능 버전이 완성될 때까지 별도 개발 브랜치나 작업용 워크트리를 새로 만들지 않는다.
- 최신 코드를 `main`에 합친 뒤 기본 체크아웃의 `main`에서 직접 수정한다.
- 이슈와 PR은 작업 경과와 피드백을 기록하는 용도로 사용한다.
- 프로토타입 개발 중에는 자동화 테스트, 타입체크, 린트, 정적 분석, 빌드를 실행하거나 완료 조건으로 삼지 않는다.
- 확인은 실행 중인 개발 서버의 실제 화면과 사용자 피드백으로 한다.
- 사용자 파일과 동시 작업 중인 변경을 덮어쓰거나 되돌리지 않는다.
